<div class="lists">
  <h1 class="lists__title">Mis listas</h1>

  <% if lists.empty? %>
    <p style="text-align: center">
      Aquí verás las listas de invitados que hayas creado. Te recomendamos que
      crees una lista por evento. Por ejemplo: <strong>Mi despedida de soltera</strong>,
      <strong>Mi despedida de soltero</strong>, <strong>Nuestra boda</strong>, etc.
    </p>
  <% end %>

  <div class="lists__btn-box">
    <%= link_to "Crear lista para evento", new_list_path, class: "btn btn--blue" %>
  </div>

  <ul>
    <% lists.each do |list| %>
      <li><%= link_to list.name, list_path(list.id), class: "lists__list-link" %></li>
    <% end %>
  </ul>
</div>
